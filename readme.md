# Cloud Controlled Rover

## Rover Local Dependencies
The Rover's local code has some dependencies
### Webcam Capture
#### All dependencies
```
sudo apt install build-essential cmake git libopencv-dev pkg-config
```
#### [OpenCV](https://opencv.org/)
#### On ubuntu linux you can install opencv with apt install
```
sudo apt update
sudo apt install libopencv-dev
```
